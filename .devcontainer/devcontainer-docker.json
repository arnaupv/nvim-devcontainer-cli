{
  "name": "Devcontainer CLI Demo",
  "build": {
    "dockerfile": "../Dockerfile",
    "args": {
      "USER_NAME": "my-app",
      "GROUP_NAME": "my-app"
    }
  },
  "remoteUser": "my-app",
  // TODO: The nvim-devcontainer-cli needs to be mounted when we are setting up the docker devcontainer. This mount will be needed until the following fetaure is not implemented.
  // "workspaceMount": "source=${localWorkspaceFolder},target=/home/my-app/.local/share/nvim/lazy/nvim-devcontainer-cli/,type=bind",
  "workspaceFolder": "/home/my-app/.local/share/nvim/lazy/nvim-devcontainer-cli/"
}
